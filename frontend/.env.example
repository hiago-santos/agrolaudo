# Em desenvolvimento, deixe vazio ou omita o arquivo: o Vite usa /api (proxy → :8000).
# Em produção (build), aponte para a URL pública da API.
# VITE_API_URL=https://sua-api.exemplo.com
VITE_API_URL=
